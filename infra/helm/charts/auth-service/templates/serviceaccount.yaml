apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "auth-service.fullname" . }}
  labels:
    {{- include "auth-service.labels" . | nindent 4 }}
  # annotations:
  #   eks.amazonaws.com/role-arn: arn:aws:iam::... # IRSA example
