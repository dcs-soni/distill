apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-review-service-config
  labels:
    app: review-service
    app.kubernetes.io/instance: {{ .Release.Name }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
