apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-review-service
  labels:
    app: review-service
    app.kubernetes.io/name: review-service
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    app: review-service
    app.kubernetes.io/instance: {{ .Release.Name }}
